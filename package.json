{
  "devDependencies": {
    "ts-loader": "^9.4.4",
    "typescript": "^5.3.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "build": "cd PlaneBuilder && webpack",
    "watch": "cd PlaneBuilder && webpack --watch",
    "build:wasm": "cd flyingcircuswasm && wasm-pack build --target bundler --out-dir ../PlaneBuilder/pkg && cd ../PlaneBuilder && npx webpack --mode production",
    "build:test": "cd flyingcircuswasm && wasm-pack build --target bundler --out-dir ../Test/pkg && cd ../Test && npx webpack --mode production",
    "build:vehicle": "cd chariotsofsteelwasm && wasm-pack build --target bundler --out-dir ../VehicleBuilder/pkg && cd ../VehicleBuilder && npx webpack --mode production"
  }
}
